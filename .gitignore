node_modules
.next
out
*.tsbuildinfo
next-env.d.ts

# generated build-time search index (regenerated by prebuild)
public/search-index.*.json
*:Zone.Identifier

# secrets
.env
.env.local
.env.*.local
.vercel
.env*.local

*.DS_Store

# source product photos (large originals, not bundled)
Hình mặt trước sau bộ điều khiển/
Gói sản phẩm/
images-origin/

# source product PDFs (large originals, not bundled)
*.pdf

# crawl scratch data (not bundled)
download/
dims-tmp.json
.claude/
