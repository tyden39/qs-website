# --- Database (Neon Postgres via Vercel Marketplace) ---
DATABASE_URL=

# --- Better Auth ---
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=
NEXT_PUBLIC_APP_URL=http://localhost:3000

# --- Vercel Blob (file uploads) ---
BLOB_READ_WRITE_TOKEN=

# --- First admin (scripts/create-admin.ts) ---
ADMIN_EMAIL=
ADMIN_PASSWORD=

# --- Email (Resend) — wired in the leads/datasheets stream ---
RESEND_API_KEY=
RESEND_FROM=

# --- Rate limit (Upstash) — wired with the contact form ---
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# --- Lead notifications (Phase 6) ---
# Email address to receive admin notifications when a new lead is submitted.
LEAD_NOTIFY_EMAIL=
